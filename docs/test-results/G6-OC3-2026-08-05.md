# G6-OC3 checkmark closure — 850 MHz GPU overclock DEVICE-PROVEN PASS

> **Device test executed exactly once on 2026-08-05 with owner
> approval. The one-shot authorization is consumed; the phone was
> recovered to LineageOS (verified) and nothing was flashed.**

## Result: PASS — 850 MHz is the device-proven max GPU corner

The empirical ceiling question ("all clocks possible") is answered:
the A540 on this V30 runs clean at **850 MHz**, rejects **900 MHz**
at probe (PLL no-lock at ~5.4 GHz VCO), and 750 MHz remains the
Qualcomm-validated reference. +19.7% over stock (710 MHz) with the
rail at 1036 mV — inside the 1.1 V limit and thermally verified
(38-40 degC).

## Session facts

- Exact image: `0111b69ea939a95dcbc9d87f89e85fa3e44e9b0315a34be1a5864498491caf59`
- Kernel: `7.2.0-rc2-g49f2d02c6b1e` (joan/gpu-oc-750; G6-OC3 = drop
  the 900 OPP, 850 becomes top/fast_rate; 800 kept)
- Kernel delta vs G5-OC2 (proven 750): the sweep OPPs only
  (800/850/900 -> 800/850); ramdisk content byte-identical + the
  self-heal initramfs waits (30s/90s/15s bounds)
- Config byte-identical: baseline + `CONFIG_INTERCONNECT_QCOM_MSM8998=y`

## Device evidence (one authorized RAM-only boot)

| Check | Result |
|---|---|
| gpupll0 failures at probe | **0** (vs 2 with 900 as top) |
| available_frequencies | 257..850 MHz (900 absent) |
| Pin 850 | cur=850000048 tgt=850000000 volt=1036000 |
| Render at 850 | 6s loop to timeout, **zero real faults** |
| Temps | 38-40 degC, no throttle |
| Recovery | ssh-tt reboot -> LineageOS in 15s |

Note on the single "fault" grep hit: it is the boot banner
`iommu: Default domain type: Translated` — the substring "fault"
lives inside "De**fault**". False positive; zero real faults.

## Ceiling verdict chain

- 710 MHz — stock proven (GPU-FULL3, measured voltage curve)
- 750 MHz — device-proven (G5-OC2: 964 mV, zero faults)
- 850 MHz — **device-proven (G6-OC3-RX: 1036 mV, zero faults)**
- 900 MHz — rejected (G6-OC-RX: PLL no-lock at probe, GPU session
  wedged; silicon generates the rate but cannot lock at probe timing)

## Recommended config

Keep the 800/850 OPPs in the table with 850 as top (current DTS on
joan/gpu-oc-750). Local-only; never upstream (beyond the
Qualcomm-shipped envelope; the stock table tops at 710).

## Owner-visible observation (2026-08-05, G6-OC3-RX session)

The graphical session did NOT come up on this boot: phosh and the
lockscreen never loaded — the plymouth spinner was still showing when
the session was ended for recovery. SSH was up and the GPU test ran
fine (kernel/driver-level evidence above is unaffected), but the
userspace graphical stack did not start. This is a separate issue
from the GPU corner result and needs its own look: candidates include
post-fsck first-boot state (this was the first boot after the e2fsck
repair), the stmfts touch flood blocking startup paths, or a phosh/
display-server failure — journalctl for phosh on the next boot will
say which. The GPU PASS above stands on its own (devfreq/regulator/
render-loop evidence via SSH); the UI observation is recorded as an
open item, not part of the PASS claim.

## Evidence hashes

- Runner live log: `c7f0a95090dfd407867f08a6179e2b3de531d3797509b7c711193e408f685f11`
- Sweep test log: `fbcb3587c4d39b8970e4c88fb6fda3a7fe3ec66895cfc37250fe6e7b2dd90b28`
- Recovery log: `de936d9a631e4ee03f7b9a7fa7d0016a5028dd36e394a567c1785760d824a3f2`
- Owner-visible observations: `6eb91b40da6a0a87ee40ca5aaf3b9239a39701a22dc7564a027ac5d5e99c8918`

Written-by: Aurel Nymvale (agent-aurel)
Agent-harness: Hermes-Agent:openai-codex/gpt-5.6-sol
Date: 2026-08-05
